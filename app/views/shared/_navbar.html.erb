<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="nav-link" href="#"><%= link_to 'Le Tuister', root_path, class: 'navbar-brand'%></a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav ml-auto">
        <% if user_signed_in? %>
            <a class="nav-link">
                <%= link_to 'Salir', destroy_user_session_path, method: :delete, class: 'nav-link' %>
            </a>
            <a class="nav-item active">
            </a>
            <a class="nav-link" href="#">Amigos</a>
        <% else %>
            <a class="nav-item active">
                <%= link_to 'Registrate', new_user_registration_path, class: 'nav-link'%>
            </a>
            <a class="nav-item active">
                <%= link_to 'Iniciar Sesión', new_user_session_path, class: 'nav-link'%>
            </a>
        <% end %>
    </div>
    <%= form_with(url: root_path, method: :get, local: :true, class: 'form-inline my-2 my-lg-0') do%>
      <%= text_field_tag :q, "", class: 'form-control mr-sm-2' %>
      <%= submit_tag("Buscar", class: 'btn btn-outline-success my-2 my-sm-0') %>
    <% end %>
  </div>
</nav>