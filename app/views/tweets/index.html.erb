<div class="container">
  <% if user_signed_in? %>
    <div>
        <%= render "form", tweet: @tweet %>
        <h2 class="text-center">Usuarios para seguir</h2>
        <%= render "users_to_follow" %>
    </div>
  <%end %>

  
  <h2 class="text-center">Tweets</h2>
  <%= render "post", tweet: @tweet %>

  <%= paginate @tweets %>
</div>

<div class="actions">
</div>

